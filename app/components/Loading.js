import React from 'react';

var Loading = React.createClass({
    render: function() {
        return (
            <div>
                Loading...
            </div>
        );
    }
});

export default Loading;