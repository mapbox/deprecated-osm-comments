import React from 'react';

var NoResults = React.createClass({
    render: function() {
        return (
            <div>
                No results found.
            </div>
        );
    }
});

export default NoResults;